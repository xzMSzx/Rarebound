PHASE 10.1.5 — FINAL NAVIGATION LOGIC REPAIR
PROJECT: RAREBOUND

CRITICAL:
The previous interaction-layer fixes succeeded partially.

Buttons are NO LONGER blocked.

Current behavior:
- buttons visually depress
- taps clearly register
- BUT navigation does nothing

Therefore:
THIS IS NO LONGER A POINTER-EVENT ISSUE.

DO NOT continue debugging z-index or overlays unless logs prove otherwise.

This patch must focus ONLY on:
1. actual click handler execution
2. navigation state flow
3. screen transition logic

NO NEW FEATURES.
NO UI REDESIGN.

────────────────────────────────────
SECTION 1 — VERIFY BUTTON EVENTS ACTUALLY FIRE
────────────────────────────────────

Add temporary diagnostics to:

- My Collection
- Market
- Stats

Log EXACTLY:
- listener attached
- click received
- function entered
- screen target found
- showScreen() called
- transition completed

I need the FULL chain.

Example structure:

[Nav]
button click received
→ openCollection()
→ collectionScreen exists?
→ showScreen(collectionScreen)
→ screen visible applied

DO NOT GUESS.

TRACE THE EXECUTION PATH.

────────────────────────────────────
SECTION 2 — INSPECT SCREEN REFERENCES
────────────────────────────────────

Strong possibility:
screen references became stale after rerenders/refactors.

Inspect:
- cached DOM refs
- querySelector timing
- dynamically recreated nodes
- detached screens
- duplicate IDs
- hidden overlays still mounted

Verify:
collectionScreen
marketScreen
statsScreen

are:
- non-null
- mounted
- unique
- visible-capable

────────────────────────────────────
SECTION 3 — AUDIT showScreen()/hideScreen()
────────────────────────────────────

Inspect the ENTIRE navigation system.

Likely failure points:
- early return
- hidden class never removed
- display:none race
- transition callback never fires
- body scroll lock deadlock
- active class mismatch
- stale state variable

Instrument:
showScreen()
hideScreen()

with FULL logs.

────────────────────────────────────
SECTION 4 — VERIFY CSS DOES NOT PREVENT VISIBILITY
────────────────────────────────────

Possible:
screen opens correctly
BUT remains:
opacity:0
visibility:hidden
display:none
translateY()

Verify:
final computed style AFTER navigation click.

────────────────────────────────────
SECTION 5 — REMOVE ANY SILENT FAILURES
────────────────────────────────────

NO empty catches.

Every navigation failure must:
console.error FULL STACK TRACE.

────────────────────────────────────
SECTION 6 — IMPORTANT ARCHITECTURAL RULE
────────────────────────────────────

DO NOT rewrite the navigation architecture.

Find the actual broken link.

This is likely:
ONE stale reference
ONE failed query
ONE early return
ONE hidden-state mismatch

NOT a full-system issue.

────────────────────────────────────
SECTION 7 — REQUIRED FINAL STATE
────────────────────────────────────

After patch:

- My Collection opens instantly
- Market opens instantly
- Stats opens instantly
- Back buttons work
- Screen transitions still animate
- No dead states
- No invisible screens
- No frozen scroll locks

────────────────────────────────────
SECTION 8 — REQUIRED FINAL REPORT
────────────────────────────────────

Must include:

1. Exact root cause
2. Exact broken function/path
3. Exact file(s) modified
4. Exact fix applied
5. Confirmation all 3 nav buttons work
6. Confirmation transitions still work
7. Confirmation runtime clean
8. Confirmation typecheck passes

DO NOT provide assumptions.
TRACE THE REAL EXECUTION FAILURE.