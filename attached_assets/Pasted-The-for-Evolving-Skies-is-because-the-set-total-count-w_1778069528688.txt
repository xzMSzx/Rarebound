The “?” for Evolving Skies is because the set total count was never hydrated/populated for that specific set, so the UI is falling back to unknown total cards.

The arrows issue is a layout stacking/viewport anchoring problem — they technically exist, but the binder grid container is still consuming too much vertical height, pushing the nav partially below the visible viewport.

Use this ultra-specific hotfix patch:

---

# PHASE 9 BINDER + SET METADATA HOTFIX

CRITICAL STABILITY PATCH — NO REDESIGNS

DO NOT:

* redesign binder
* redesign collection screen
* change pagination style language
* alter economy systems
* touch market systems
* touch vendor systems

ONLY FIX:

1. Evolving Skies “?” metadata issue
2. binder navigation buttons clipped offscreen
3. binder grid consuming too much height

Maintain:

* existing premium collector aesthetic
* current spacing language
* current animations
* current overlay system

---

SECTION 1 — FIX EVOLVING SKIES “?” TOTAL COUNT
(HIGH PRIORITY)
---------------

Root issue:
The set metadata object for Evolving Skies is missing:

* totalCards
  OR
* secretRareCount

The collection progress UI therefore falls back to:
“9 / ?”

---

## SECTION 2 — ENSURE ALL SETS HAVE COMPLETE METADATA

Locate:

* set registry
* set metadata map
* packDefinitions
  OR equivalent source of set information

Ensure EVERY set contains:

{
id,
name,
totalCards,
secretRareCount,
symbol/logo/packArt
}

Specifically add/fix:

Evolving Skies:

* totalCards: 237
* secretRareCount: 34

Verify:
Lost Origin,
Paldea Evolved,
151,
Paldean Fates,
etc
all resolve properly.

DO NOT hardcode only in UI.
Fix the source metadata properly.

---

## SECTION 3 — COLLECTION SCREEN FALLBACK SAFETY

Add defensive fallback:

If totalCards is missing:

* never render “?”

Instead:

* render “--”
  OR
* gracefully hide percentage

But ideally metadata fix alone resolves this.

---

SECTION 4 — BINDER NAV ROOT ISSUE
(HIGH PRIORITY)
---------------

Current issue:
The pagination controls are STILL positioned partially below the visible viewport.

This means:
the binder grid section is still taller than the available screen space.

The nav is being pushed downward instead of anchored.

---

## SECTION 5 — FORCE TRUE VIEWPORT LAYOUT

For #binder-screen:

Use:

height: 100dvh;

NOT:
100vh

Reason:
mobile Safari/iOS browser UI changes viewport height dynamically.
100vh becomes inaccurate.

100dvh fixes this.

---

## SECTION 6 — PROPER FLEX CONSTRAINTS

Ensure exact hierarchy:

#binder-screen
→ display:flex
→ flex-direction:column
→ overflow:hidden

Header:
flex-shrink:0

Grid wrapper:
flex:1
min-height:0
overflow:hidden

Pagination:
flex-shrink:0

This structure is CRITICAL.

---

## SECTION 7 — BINDER GRID SCROLLING

ONLY the binder grid area may scroll.

NOT:
entire screen.

Apply:

overflow-y:auto;

to:
the grid container ONLY.

This ensures:
pagination remains permanently visible.

---

## SECTION 8 — REMOVE EXCESS BOTTOM SPACER

There is still invisible spacing below the 3rd binder row.

Find:

* margin-bottom
* padding-bottom
* spacer div
* safe-area duplication
* pseudo-element spacing

Reduce/remove it.

The gap between:
last card row
AND
pagination controls

should be tight and intentional.

---

## SECTION 9 — PAGINATION VISIBILITY BOOST

Current arrows visually disappear into the background.

Improve subtly:

.binder-nav button {
background: rgba(255,255,255,0.08);
border: 1px solid rgba(255,255,255,0.14);
}

Arrow icon/text:
increase opacity slightly.

DO NOT:
make bright/glowing buttons.

Goal:
premium readability.

---

## SECTION 10 — SAFE AREA CORRECTION

Current safe-area padding may be duplicated.

Pagination container should use ONLY:

padding-bottom:
calc(env(safe-area-inset-bottom, 0px) + 8px);

Avoid stacking:
extra bottom margins + safe area.

---

## SECTION 11 — TOUCH TARGETS

Ensure arrows:
remain visually minimal,
BUT actual tap target is:

44x44 minimum.

Use invisible padding if needed.

---

## SECTION 12 — FINAL EXPECTED RESULT

After patch:

Collection screen:

* all set counts display correctly
* no “?” metadata

Binder:

* arrows always fully visible
* arrows easy to tap
* pagination anchored properly
* binder cards scroll independently
* no excessive dead gap below rows
* layout feels stable on iOS Safari

CRITICAL:
Do NOT redesign anything.
This is a structural viewport/layout correction patch only.
