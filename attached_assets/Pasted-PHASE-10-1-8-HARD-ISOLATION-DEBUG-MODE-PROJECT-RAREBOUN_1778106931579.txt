PHASE 10.1.8 — HARD ISOLATION DEBUG MODE
PROJECT: RAREBOUND
MODEL: REPLIT POWER

CRITICAL:
We are NO LONGER doing speculative fixes.

Previous patches identified REAL issues:
- hidden fullscreen modals
- CSS specificity conflicts
- iOS click synthesis problems

BUT:
the interaction system is STILL unreliable.

At this point:
we enter HARD ISOLATION DEBUG MODE.

This patch is NOT about:
- adding features
- polishing UI
- redesigning architecture

This patch exists ONLY to:
ISOLATE THE EXACT SUBSYSTEM CAUSING GLOBAL INTERACTION FAILURE.

You are REQUIRED to debug systematically.

NO MORE GUESSING.

────────────────────────────────────
SECTION 1 — IMPORTANT PHILOSOPHY
────────────────────────────────────

DO NOT:
patch random CSS
patch random z-index
patch random pointer-events
patch random overlays

We now use:
CONTROLLED SUBSYSTEM ELIMINATION.

Goal:
Find EXACTLY which system breaks interactions.

Once isolated:
repair surgically.

────────────────────────────────────
SECTION 2 — CREATE ISOLATED DEBUG BUTTON
────────────────────────────────────

Create ONE completely isolated debug button.

Requirements:
- appended directly to document.body
- NOT inside any screen
- NOT inside any overlay
- NOT inside vendor hub
- NOT using existing button classes
- NOT using existing navigation system

Implementation:

const debugBtn = document.createElement('button');

Style:
- position: fixed
- top: 120px
- left: 20px
- z-index: 999999
- width: 120px
- height: 48px
- background: red
- color: white

Text:
DEBUG TAP

Interaction:
onclick → alert('DEBUG BUTTON WORKS')

IMPORTANT:
This button exists ONLY for diagnosis.

────────────────────────────────────
SECTION 3 — DIAGNOSTIC INTERPRETATION
────────────────────────────────────

After adding the button:

CASE A:
Debug button ALSO fails
→ browser-level/global interaction suppression exists

CASE B:
Debug button works
→ app interaction architecture specifically is broken

This distinction is CRITICAL.

────────────────────────────────────
SECTION 4 — SUBSYSTEM ISOLATION STRATEGY
────────────────────────────────────

If the debug button works:
begin controlled subsystem elimination.

TEMPORARILY DISABLE systems ONE AT A TIME.

DO NOT delete code.
Use feature flags or temporary early returns.

After each disable:
test:
- My Collection
- Binder back
- Market
- Page arrows
- Vendor buttons

The MOMENT interactions return:
the disabled subsystem is the culprit.

────────────────────────────────────
SECTION 5 — REQUIRED ISOLATION ORDER
────────────────────────────────────

Disable IN THIS EXACT ORDER:

STEP 1:
Utility Dock
- disable rendering entirely
- remove all dock listeners

TEST.

STEP 2:
Ambient Audio System
- disable unlockAudioContext
- disable global pointer listeners
- disable IntersectionObserver audio hooks

TEST.

STEP 3:
Scroll Manager
- disable ALL scroll locking
- disable ALL touchmove prevention
- disable body fixed locking

TEST.

STEP 4:
Boot Screen
- bypass boot entirely
- immediately render app

TEST.

STEP 5:
Help/Settings Screens
- completely remove mounting
- do not render them

TEST.

STEP 6:
Card Detail Modal
- disable mounting entirely

TEST.

STEP 7:
Sell Modal
- disable mounting entirely

TEST.

STEP 8:
Global Touch Helpers
- disable iosTap()
- disable custom touch handlers
- use native click ONLY

TEST.

────────────────────────────────────
SECTION 6 — GLOBAL EVENT TRACE
────────────────────────────────────

While isolating:
maintain TEMP diagnostics.

Log:
- pointerdown
- pointerup
- touchstart
- touchend
- click

for:
- debug button
- My Collection
- Binder Back

We need TRUE event flow.

────────────────────────────────────
SECTION 7 — PREVENTDEFAULT AUDIT
────────────────────────────────────

Search ENTIRE CODEBASE for:
- preventDefault(
- stopPropagation(
- stopImmediatePropagation(
- setPointerCapture(
- releasePointerCapture(

Document:
- exact file
- exact line
- exact reason

Especially inspect:
- iosTap
- scrollManager
- utility dock
- overlays
- modals
- audio unlock logic

One incorrect preventDefault may be killing click synthesis globally.

────────────────────────────────────
SECTION 8 — IMPORTANT RULE
────────────────────────────────────

DO NOT "fix" things during isolation.

Only:
disable systems temporarily.

Goal:
find the FIRST subsystem whose removal restores interaction.

That subsystem is the culprit.

────────────────────────────────────
SECTION 9 — IF CULPRIT FOUND
────────────────────────────────────

Once the culprit subsystem is identified:

ONLY THEN:
repair the exact cause surgically.

NO large refactors.

────────────────────────────────────
SECTION 10 — REQUIRED FINAL REPORT
────────────────────────────────────

Must include:

1. Whether isolated debug button worked
2. Exact subsystem causing interaction collapse
3. Exact file(s) responsible
4. Exact listener/layer causing failure
5. Exact preventDefault/overlay/handler responsible
6. Which isolation step restored interaction
7. Exact final repair applied
8. Confirmation ALL buttons now work
9. Confirmation runtime clean
10. Confirmation typecheck passes

NO speculation.
NO theory.
ONLY isolated proven causes.