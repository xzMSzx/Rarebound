PROJECT UPDATE — PHASE 4.2 REAL SET SYSTEM + 10 CARD PACK FOUNDATION

This update upgrades the Phase 4 Pokémon card system to use the correct card sets and prepares the engine for realistic 10-card booster packs.

The existing engine RNG, pity system, animation system, swipe system, and overlay controller must remain unchanged.

This update only modifies the Pokémon data layer and pack generation logic.

---

CRITICAL RULE

Do NOT modify:

engine RNG logic
pity system
overlay animation system
skip summary logic
Monte Carlo simulation

Only modify the Pokémon card integration layer.

---

SECTION 1 — UPDATE SET LIST

Replace the existing set list with the following five sets.

Each set must be selectable in the UI.

SETS:

Evolving Skies
setId: evs

Scarlet & Violet — 151
setId: sv3pt5

Paldean Fates
setId: sv04pt5

Paldea Evolved
setId: sv2

Lost Origin
setId: swsh11

---

SECTION 2 — REPLACE DROPDOWN WITH PACK TILE SELECTOR

Remove the dropdown UI.

Create a pack selector panel with five selectable tiles.

Example layout:

[ Evolving Skies ]
[ Scarlet & Violet 151 ]
[ Paldean Fates ]
[ Paldea Evolved ]
[ Lost Origin ]

Each tile should show:

pack name
small Pokémon logo
subtle card background

When tapped:

loadSet(setId)

Highlight the active set.

---

SECTION 3 — UPDATE API FETCHING

File: data/setLoader.js

Modify fetchSetCards() to request the full card set.

Endpoint:

https://api.pokemontcg.io/v2/cards?q=set.id:{setId}&pageSize=250

Return the array:

response.data

Each card object should contain:

name
rarity
images.large

---

SECTION 4 — IMPROVE RARITY MAPPING

File: data/rarityMapper.js

Replace the old mapping with the following mapping table.

Common → common
Uncommon → common

Rare → rare

Rare Holo
Rare Holo V
Rare Holo VMAX
Rare Holo GX
Rare Holo EX
Double Rare
Illustration Rare
Trainer Gallery Rare

→ epic

Ultra Rare
Special Illustration Rare
Hyper Rare
Secret Rare

→ legendary

If rarity is missing:

return common

---

SECTION 5 — BUILD CARD POOLS BY RARITY

File: data/cardPoolManager.js

Modify loadSet() so cards are separated into pools.

Structure:

cardPools = {
common: [],
rare: [],
epic: [],
legendary: []
}

Loop through fetched cards:

mappedRarity = mapPokemonRarity(card.rarity)

Push card into the matching pool.

Example stored card:

{
name: card.name,
rarity: mappedRarity,
imageUrl: card.images.large
}

Cache pools by setId.

---

SECTION 6 — PREPARE FOR 10 CARD PACKS

Update the pack generation logic so each pack contains 10 cards.

Slots should be generated in the following structure:

Slot 1 → common
Slot 2 → common
Slot 3 → common
Slot 4 → common

Slot 5 → rare

Slot 6 → rare

Slot 7 → epic

Slot 8 → epic

Slot 9 → epic

Slot 10 → legendary hit slot

Legendary slot should roll probability:

80% epic
15% legendary
5% secret/ultra

Pull the correct card from the cardPools.

---

SECTION 7 — UPDATE CARD AUGMENTATION

File: main.js

Modify augmentCards() so it supports 10 cards.

Each card returned from the engine must be augmented with:

name
imageUrl

using getRandomCard(rarity)

---

SECTION 8 — UPDATE REVEAL UI

Modify revealCard() so it displays the real card image.

Create:

img.src = card.imageUrl

Apply existing rarity glow effects to the container.

---

SECTION 9 — UPDATE SKIP SUMMARY

Modify showSkipSummary() so each tile shows the card image.

Tile layout:

10 cards in a row or grid.

Cards must be larger than the current summary tiles.

---

SECTION 10 — UPDATE COLLECTION GRID

Update collection tiles to show:

card image
card name

Add lazy loading to images.

Example class:

grid-card-img

---

SECTION 11 — SET LOADING FLOW

When a set tile is selected:

disable pack opening
display loading text

Example:

Loading Evolving Skies...

When loading completes:

enable pack opening
display confirmation

Example:

✓ Evolving Skies ready

---

SECTION 12 — ERROR HANDLING

If API fails:

display:

Failed to load card set.

Add retry button.

---

EXPECTED RESULT

User selects a Pokémon set from pack tiles.

Cards are fetched from the Pokémon API.

Cards are separated into rarity pools.

Opening a pack generates 10 cards.

Reveal animation shows real Pokémon card artwork.

Skip summary and collection grid display real card images.

The simulator now behaves like a realistic Pokémon booster pack opener.

EN
