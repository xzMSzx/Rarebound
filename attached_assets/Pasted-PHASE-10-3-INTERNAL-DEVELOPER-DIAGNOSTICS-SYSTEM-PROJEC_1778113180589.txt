PHASE 10.3 — INTERNAL DEVELOPER DIAGNOSTICS SYSTEM
PROJECT: RAREBOUND
MODEL: REPLIT POWER

IMPORTANT:
The app is now VERIFIED STABLE on real iOS hardware.

This patch is NOT a public feature expansion.

This is an INTERNAL MAINTENANCE + DIAGNOSTICS SYSTEM.

Goal:
convert the temporary debugging infrastructure into a permanent hidden developer toolkit integrated cleanly into the existing Developer Access system.

DO NOT expose this to normal users.
DO NOT make this feel like a hacker/debug app.
DO NOT clutter the production UI.

Tone:
minimal
professional
internal
premium

────────────────────────────────────
SECTION 1 — CORE GOAL
────────────────────────────────────

We already have:

* Developer Access
* Archive Utilities
* internal debug infrastructure
* subsystem isolation flags
* diagnostics helpers

Currently:
these are fragmented across:

* query params
* console utilities
* hidden debug code

We now want:
ONE cohesive hidden diagnostics system.

────────────────────────────────────
SECTION 2 — SETTINGS INTEGRATION
────────────────────────────────────

Inside:
Settings
→ Developer Access
→ Archive Utilities

Add new collapsible section:

Title:
System Diagnostics

Collapsed by default.

STYLE:

* visually subtle
* dark premium panel
* thin muted borders
* small typography
* restrained gold accents

DO NOT:
use bright debug colors
use giant toggles
make it look unfinished

────────────────────────────────────
SECTION 3 — DEBUG MODE TOGGLE
────────────────────────────────────

Add master toggle:

Developer Diagnostics

When OFF:

* no debug logs
* no traces
* no overlay diagnostics
* no floating diagnostics
* no extra instrumentation

When ON:

* internal diagnostics activate
* debug logs allowed
* debug overlay available
* subsystem tools unlocked

Persist:
tcg_dev_diagnostics

IMPORTANT:
Normal users should NEVER accidentally enable this.

Developer Access unlock required.

────────────────────────────────────
SECTION 4 — DIAGNOSTIC OVERLAY
────────────────────────────────────

When diagnostics enabled:
allow a SMALL floating overlay panel.

NOT fullscreen.

Placement:
bottom-left OR bottom-right.

STYLE:

* semi-transparent dark glass
* tiny monospace text
* low opacity
* compact
* professional

Content examples:
FPS
Current screen
Active overlays count
Scroll lock count
Current vendor ambience
AudioContext state
Active modals
Current z-index layer count

VERY lightweight.

────────────────────────────────────
SECTION 5 — INTERACTION DIAGNOSTICS
────────────────────────────────────

Add optional toggles:

* Touch Trace
* Nav Audit
* Overlay Audit
* Audio Diagnostics

These should:
enable the existing internal systems.

DO NOT recreate them from scratch.

Only activate when:
Developer Diagnostics ON.

────────────────────────────────────
SECTION 6 — SUBSYSTEM ISOLATION TOOLS
────────────────────────────────────

Integrate the existing isolation flags into UI toggles.

Add toggles for:

* Disable Utility Dock
* Disable Ambient Audio
* Disable Scroll Manager
* Disable Boot Screen
* Disable Overlays
* Disable iosTap helper

IMPORTANT:
These are INTERNAL ONLY.

When toggled:
apply the SAME logic currently used by:
?nodock=1
?noaudio=1
?noscroll=1
etc.

DO NOT duplicate architecture.

Reuse existing debugFlags system.

────────────────────────────────────
SECTION 7 — SAFE ISOLATION DESIGN
────────────────────────────────────

Subsystem toggles should:
require app refresh to apply.

Reason:
prevents runtime instability from hot-disabling systems mid-session.

UX:
show subtle notice:

"Requires reload to apply."

Add:
Reload App button.

────────────────────────────────────
SECTION 8 — INTERNAL DIAGNOSTIC LOGGING
────────────────────────────────────

When diagnostics enabled:
allow:

* NavBinding logs
* TouchTrace logs
* GlobalClick logs
* Audio transition logs

When disabled:
production console remains clean.

DO NOT:
spam logs unnecessarily.

────────────────────────────────────
SECTION 9 — DEBUG TAP BUTTON
────────────────────────────────────

DO NOT permanently delete DEBUG TAP infrastructure.

Integrate it cleanly.

When:
Developer Diagnostics ON

show:
small floating "DEBUG TAP" button.

When OFF:
completely absent.

Use existing implementation.

────────────────────────────────────
SECTION 10 — ARCHIVE UTILITIES EXPANSION
────────────────────────────────────

Inside Developer Access:
keep existing tools:

* Add Funds
* Infinite Balance
* Force Vendor Refresh
* Force Market Refresh
* Force Broker Arrival
* Reset Economy
* Clear Market History

Add:
small Diagnostics subsection divider.

────────────────────────────────────
SECTION 11 — PERFORMANCE + SAFETY
────────────────────────────────────

CRITICAL:
Diagnostics must:

* NOT run in production mode by default
* NOT leak memory
* NOT create runaway observers
* NOT duplicate listeners
* NOT permanently attach traces

Diagnostics should activate lazily ONLY when enabled.

────────────────────────────────────
SECTION 12 — VISUAL DESIGN RULES
────────────────────────────────────

This should feel:
like internal archive tooling.

NOT:
developer console spam.

Think:
premium hidden admin utilities.

Use:

* subtle typography
* restrained glow
* compact spacing
* soft glassmorphism
* muted colors

────────────────────────────────────
SECTION 13 — IMPORTANT RESTRICTIONS
────────────────────────────────────

DO NOT:
rewrite interaction architecture
rewrite overlays
rewrite audio
rewrite screen manager
rewrite vendor system

This patch only ORGANIZES existing debugging infrastructure cleanly.

────────────────────────────────────
SECTION 14 — REQUIRED FINAL VERIFICATION
────────────────────────────────────

Verify:

NORMAL MODE:

* zero debug UI
* zero debug logs
* clean production experience

DEVELOPER MODE:

* diagnostics panel appears
* traces work
* isolation toggles work
* reload behavior works
* debug tap appears
* no interaction instability introduced

GENERAL:

* all app interactions still stable
* no memory leaks
* no runtime errors
* no overlay deadlocks

────────────────────────────────────
SECTION 15 — REQUIRED FINAL REPORT
────────────────────────────────────

Must include:

1. Exact diagnostics integrated
2. Exact debug systems preserved
3. Exact UI added
4. Whether isolation toggles reused existing flags
5. Whether diagnostics remain hidden from normal users
6. Confirmation production mode clean
7. Confirmation developer mode works
8. Confirmation runtime clean
9. Confirmation typecheck passes

DO NOT expose diagnostics publicly.
This is an INTERNAL maintenance toolkit.
